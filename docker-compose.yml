services:
  postgres:
    build: .
    container_name: ckd-workshop
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U workshop -d ckd"]
      interval: 5s
      retries: 5
